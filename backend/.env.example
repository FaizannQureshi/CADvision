# Copy to .env and fill in. Never commit .env.
OPENAI_API_KEY=

# Optional: comma-separated browser origins allowed to call the API (for split frontend/API deploys)
# CORS_ORIGINS=https://your-app.pages.dev,http://localhost:5173
